package com.example.unravel.data.remote.entity

class ZippedData( val highlightData : String, val includedData : String) {

    init {
        val highlightResData : String = highlightData
        val includedResData : String = includedData
    }
}